import time
import os
import mmap
import signal
import posix_ipc
import sys
import struct

def unroll(args, func, method, res):
    if method == 'proc':

    elif method == 'thre':

    else:
        print("method " + method + " unknown!")
        return

res = []
